<section>
    <% if current_user.posts.exists?(@post.id) %>
        <%= link_to 'Edit Post', post_path(@post), method: :edit %><br>
        <%= link_to 'Remove', post_path(@post), method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>
</section>